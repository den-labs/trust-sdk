{
  "name": "@denlabs/trust-sdk-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "Multichain trust oracle SDK monorepo — shared core + per-chain wrappers",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch"
  },
  "author": "DenLabs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/den-labs/trust-sdk.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "viem": "^2.21.0",
    "vitest": "^2.0.0"
  }
}
